# Memo: Kube Copilot Project Status & Roadmap

Michael Tanner, April 25, 2025

## 1. Purpose  
Kube Copilot (abbreviated **kc**) aims to let engineers interact with Kubernetes and related tooling through natural-language chat. The project is fully open-source and currently hosted on GitHub (at `kubecopilot.net` and `https://github.com/michael-tanner/kube-copilot`).

## 2. Licensing  (TBD)
- Evaluate permissive licenses (MIT, Apache-2.0, BSD-3-Clause, etc.) and select one that aligns with community adoption and potential commercial use.

## 3. Current State  
| Area | Status |
|------|--------|
| **Language** | Go |
| **Execution** | Optimized for a VS Code **dev-container**; uses a Docker image with all required CLIs. |
| **CLI** | `kc` command works for basic queries (e.g., “List namespaces and running pods”). |
| **Dependencies** | `kubectl`, Helm (integration pending). |
| **Backend API** | Serves the CLI today; designed to power a future web UI. |
| **Project structure** | `.kubecopilot/` folder holds a YAML config plus a `workspace/` sub-folder for local files. |

## 4. Immediate Roadmap  
1. **File-System Integration**  
   - Finalize API functions that read/write the `.kubecopilot/workspace` files.  
   - Extend LLM tools to use those functions.
   - Why? One example: this will allow `kc` to read yaml files saved by kubectl and then take actions.

2. **Helm Support**  
   - Enable chat-driven Helm installs/upgrades of popular charts. 
   - Enable natural language queries to scan Helm Charts and Kubernetes resources to answer complex questions.

3. **Testing Strategy**  
   - **Unit tests**: expand coverage with Go test suites.  
   - **Integration tests**:  
     - Spin up a lightweight Kubernetes cluster (kind or minikube) in GitHub Actions.  
     - Inject an OpenAI API key securely.  
     - Ensure tests tear down resources to control costs.

4. **Web Interface**  
   - Build a static React front-end that communicates with the REST API.  
   - Host either from the cluster or locally via the same dev container.

## 5. Longer-Term Ideas  
- **End-to-End Dev Workflow**  
  - Let the LLM clone repos, modify code, build Docker images, and deploy them into a dedicated dev node, then run tests and open pull requests automatically.

- **Dapr & Radius Integration**  
  - Explore how `kc` can bootstrap [Dapr](https://github.com/dapr) or Microsoft [Radius](https://github.com/radius-project), giving users opinionated patterns for microservices and platform engineering.

- **Security & Observability**  
  - Plug in security-scanning tools leveraging **[Drasi](https://github.com/drasi-project)** for continuous change monitoring.  
  - Evaluate Grafana’s approach to dashboards and alerts.

- **Copilot into devcontainer on local or remote image**
  - This idea is forward looking and aims to allow KubeCopilot to be the driver in another container running
  a local devenviornment. This way KubeCopilot has locus of control, yet the human copilot would see and approve code changes in a local instance of VS Code.
  - With a full-featured KubeCopilot Web UI, this would also work remotely with cloud-based devcontainer images.

## 6. Open Questions  
1. Which open-source license?
2. Establish testing environments in Azure and AWS while keeping costs low.
3. How can we standardize integration tests that rely on both Kubernetes and OpenAI with minimal flake risk?
